{{!< default}}

<h1 style="text-align: center;" id="posts-headline">文章分类</h1>
{{#get "tags" limit="all"}}
    <ul class="tags">
        {{#foreach tags}}
            <li>
                <a href="{{url}}">{{name}}</a>
            </li>
        {{/foreach}}
    </ul>
{{/get}}
